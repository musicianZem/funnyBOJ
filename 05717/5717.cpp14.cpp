#include <iostream>
#include <cstdio>
using namespace std;

int N;
int setMenu[ 1000 ];
int bestValue[ 1000 ];
int main() {
	while( 1 ) {
		int a, b; cin >> a >> b;
		if( a == 0 && b == 0 ) break;
		cout << a + b << endl;
	}

}